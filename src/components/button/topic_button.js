import React from 'react';
export function Topic_button({topic}) {
    return (
        <div>
            <button className="subject_topic_btn">
                {topic}
            </button>
        </div>

    );
}